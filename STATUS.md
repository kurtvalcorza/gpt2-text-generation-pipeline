# Release status

Current status: **Candidate** — tutorial source complete, awaiting clean-runtime execution. The pipeline package with its adaptation contract (`evaluate`, `unigram_baseline`, `adapt`, `save_artifact` / `from_artifact`; the pinned SciTLDR-A abstract corpus and BYOD loaders in `samples.py`; perplexity and the unigram floor in `metrics.py`), offline unit tests (34, no weights required, plus 5 notebook-parity tests and 2 model-backed tests that run when the snapshot is staged), a local CPU execution of the committed notebook blob in the Windows venv (frozen test perplexity 40.17 → adapted 34.73 against a unigram floor of 1,720.9; recorded in `docs/release-verification.md`), `MODEL_CARD.md` (MODEL_CARD_SPEC 1.1), and the standalone `E2E` tutorial notebook `tutorials/gpt2_text_generation_colab.ipynb` (generated by `tools/build_notebook.py`, DIMER Notebook Specification 2.0 §4) with its static validator, generator parity checks and CI workflow exist. Unit tests and static conformance checks are expected to run in GitHub Actions. The earlier `TASK-INFERENCE` notebook's Kaggle run (blob `263771047488`, commit `f4020ce`) is retained as history and is not evidence for the `E2E` blob. The tutorial remains non-release-grade until clean-runtime execution evidence for the exact candidate commit is recorded according to `docs/release-verification.md`.
